# Copy this file to infra/.env for local development

# Scoring service
PORT=8081
TEC_RAG_BASE_URL=https://your-edgecloud-gateway/api
TEC_RAG_API_KEY=supersecret
TEC_CHAT_ID=theta-kb-01

# Shared infra
PROJECT_ID=my-first-project
REDIS_URL=redis://redis:6379
POSTGRES_URL=postgresql://ally:secret@postgres/allyhub

# Discord bot (ingestion)
DISCORD_BOT_TOKEN=
DISCORD_ALLOWED_GUILDS=
DISCORD_ALLOWED_CHANNELS=
DISCORD_INCLUDE_BOTS=false
DISCORD_MIN_MESSAGE_LENGTH=1

# Sentiment service
# HF model selection
SENTIMENT_MODEL_ID=cardiffnlp/twitter-roberta-base-sentiment-latest
# Alternative examples:
# SENTIMENT_MODEL_ID=finiteautomata/bertweet-base-sentiment-analysis
# SENTIMENT_MODEL_ID=distilbert-base-uncased-finetuned-sst-2-english

# spaCy model (installed at build)
SPACY_MODEL=en_core_web_sm

# Optional: private models
HF_TOKEN=

# Performance
INFERENCE_DEVICE=cpu        # cpu | cuda
MAX_BATCH=64
LOG_LEVEL=info

# Optional: custom entities (JSONL patterns for spaCy EntityRuler)
# Each line: {"label":"PROJECT","pattern":[{"LOWER":"theta"}]}
CUSTOM_ENTITIES_FILE=./infra/custom_entities.jsonl

# Shade Agent Configuration (MAINNET)
# NEAR / Agent
NEAR_NETWORK=mainnet                    # mainnet for production
NEAR_RPC_URL=https://rpc.mainnet.near.org
AGENT_ACCOUNT=rewards.ally.near         # Replace with your actual NEAR mainnet account
AGENT_ADMIN=admin.ally.near             # Replace with your admin account

# Agent API
SHADE_AGENT_HTTP_PORT=8090
SHADE_AGENT_TOKEN=your-super-secret-token-here  # Generate a long random token

# NEAR Account Setup (for shade-agent-cli)
NEAR_ACCOUNT_ID=rewards.ally.near       # Your NEAR mainnet account ID
NEAR_SEED_PHRASE=                       # Your 12-word seed phrase (keep secret!)
API_CODEHASH=                           # Will be generated by shade-agent-cli
APP_CODEHASH=                           # Will be generated by shade-agent-cli
NEXT_PUBLIC_contractId=ac-sandbox.rewards.ally.near  # For mainnet deployment
NEAR_RPC_JSON={"jsonrpc":"2.0","id":"dontcare","method":"status","params":[]}

# Theta Blockchain Configuration
THETA_RPC_URL=https://eth-rpc-api.thetatoken.org/rpc